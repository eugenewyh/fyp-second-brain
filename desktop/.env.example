# Vite env for desktop (optional)

# Better Auth service (see ../auth). Empty = Account OTP disabled in UI.
# Local: http://localhost:3000
# Production (Path A): https://YOUR-SERVICE.onrender.com
VITE_AUTH_URL=http://localhost:3000
